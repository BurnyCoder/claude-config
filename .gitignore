# Credentials and secrets
.credentials.json

# History and cache (large/sensitive)
history.jsonl
cache/
paste-cache/
shell-snapshots/
session-env/
debug/
file-history/
statsig/
telemetry/
stats-cache.json

# IDE-specific
ide/

# Project-specific data (may contain sensitive paths/info)
projects/

# Temporary data
todos/
plans/
downloads/
tasks/

# Plugins - track config, ignore cache/cloned repos
plugins/cache/
plugins/marketplaces/
plugins/install-counts-cache.json

# External skill repos (managed separately)
skills/tauri/
